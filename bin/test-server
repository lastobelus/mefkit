source .env.test

echo "PORT: $PORT"

bundle exec rails webpacker:compile

if command -v overmind >/dev/null 2>&1; then
  OVERMIND_ENV=.env.test overmind start -s .overmind.test.sock -f Procfile.test -l web,webpacker,cypress
else
  PORT=7780 heroku local -f Procfile.test
fi